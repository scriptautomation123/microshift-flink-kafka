OPENSHIFT_NAMESPACE=kafka-dev

# image reference for OpenShift internal registry or external registry
KAFKA_IMAGE=image-registry.openshift-image-registry.svc:5000/kafka-dev/kafka-kraft:3.7.1
KAFKA_VERSION=3.7.1
SCALA_VERSION=2.13

# generate with scripts/generate-kraft-cluster-id.sh
KAFKA_CLUSTER_ID=

KAFKA_REPLICAS=3
KAFKA_STORAGE_CLASS=
KAFKA_STORAGE_SIZE=50Gi

# broker defaults
KAFKA_DEFAULT_REPLICATION_FACTOR=3
KAFKA_MIN_INSYNC_REPLICAS=2
KAFKA_NUM_PARTITIONS=6

# Optional: credentials used for registry login and image pull secret creation.
REGISTRY_LOGIN_USERNAME=
REGISTRY_LOGIN_PASSWORD=
REGISTRY_PULL_SECRET_NAME=ghcr-pull
